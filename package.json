{
  "name": "project-app-copy",
  "version": "1.0.0",
  "description": "Project App Copy Backend",
  "main": "backend/src/server.js",
  "scripts": {
    "start": "cd backend && npm install && npm start",
    "dev": "cd backend && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
